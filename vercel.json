{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "curl -fsSL https://bun.sh/install | bash && ~/.bun/bin/bun install && export BUN_INSTALL=\"$HOME/.bun\" && export PATH=$BUN_INSTALL/bin:$PATH && echo $PATH",
  "buildCommand": "~/.bun/bin/bun run build"
}
