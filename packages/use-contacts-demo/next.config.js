/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    output: 'export',
    basePath: '/use-contacts',
    assetPrefix: '/use-contacts/'
}

export default nextConfig