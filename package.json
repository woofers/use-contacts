{
  "name": "use-contacts",
  "version": "1.2.0",
  "description": "Typesafe Contact Picker API wrapper for React",
  "files": ["index.js", "package.json", "README.md"],
  "scripts": {
    "deploy": "bun run --cwd=packages/use-contacts deploy",
    "build-site": "turbo run build-site",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "format": "biome format --write"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "workspaces": ["packages/*"],
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "turbo": "^1.10.15"
  }
}
